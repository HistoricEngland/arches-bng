
services:
    nginx-arches-bng-0941b:
      container_name: nginx-arches-bng-0941b
      image: nginx:latest
      ports:
        - "8080:80"
      volumes:
        - ./docker/nginx-config/default.conf:/etc/nginx/conf.d/default.conf
      networks:
        - arches-bng-0941b-network

    db-arches-bng-0941b:
      container_name: db-arches-bng-0941b
      image: postgis/postgis:14-3.2
      volumes:
        - ../../arches_bng-postgres-data:/var/lib/postgresql/data
        - ../../arches_bng-postgres-log:/var/log/postgresql
        - ./docker/init-unix.sql:/docker-entrypoint-initdb.d/init.sql # to set up the DB template
      ports:
        - '5433:5432'
      env_file: 
        - ./docker/env_file.env
      networks:
        - arches-bng-0941b-network

    elastic-bindmount-init-arches-bng-0941b:
      image: alpine
      container_name: elastic-bindmount-init-arches-bng-0941b
      restart: "no"
      entrypoint: |
        /bin/sh -c "chown 1000:1000 /esBindMount"
      volumes:
        - ../../arches_bng-elasticsearch-data:/esBindMount


    elasticsearch-arches-bng-0941b:
      container_name: elasticsearch-arches-bng-0941b
      image: elasticsearch:8.5.0
      volumes:
        #- ../../elasticsearch_data_arches_bng:/usr/share/elasticsearch/data
        - ../../arches_bng-elasticsearch-data:/usr/share/elasticsearch/data
      ports:
        - "9201:9200"
        - "9301:9300"
      env_file: 
        - ./docker/env_file.env
      networks:
        - arches-bng-0941b-network
      depends_on:
        - elastic-bindmount-init-arches-bng-0941b

    rabbitmq-arches-bng-0941b:
      container_name: rabbitmq-arches-bng-0941b
      image: rabbitmq:3.8-management
      hostname: my-rabbit
      volumes:
        - rabbitmq-data-0941b:/var/lib/rabbitmq/mnesia/rabbit@my-rabbit
        - rabbitmq-logs-0941b:/var/log/rabbitmq/log
      ports:
        - 5673:5672
        - 15673:15672
      networks:
        - arches-bng-0941b-network

    #pg-feat-arches-bng:
    #  container_name: pg-feat-arches-bng
    #  image: pramsey/pg_featureserv
    #  volumes:
    #    - ../../pg_featureserv_config_arches_bng:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-arches-bng:5432/arches_bng
    #  ports:
    #    - 9000:9000
    #  networks:
    #    - arches-bng-network
    #  depends_on:
    #    - db-arches-bng
    
    #pg-tile-arches-bng:
    #  container_name: pg-tile-arches-bng
    #  image: pramsey/pg_tileserv
    #  volumes:
    #    - ../../pg_tileserv_config_arches_bng:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-arches-bng:5432/arches_bng
    #  ports:
    #    - 7800:7800
    #  networks:
    #    - arches-bng-network
    #  depends_on:
    #    - db-arches-bng


networks:
  arches-bng-0941b-network:
    name: arches-bng-0941b-network
    driver: bridge

volumes:
    rabbitmq-logs-0941b:
    rabbitmq-data-0941b:
